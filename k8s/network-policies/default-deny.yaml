# Default deny all ingress and egress traffic in the app1 namespace.
# All traffic must be explicitly allowed by other network policies.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: app1
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
